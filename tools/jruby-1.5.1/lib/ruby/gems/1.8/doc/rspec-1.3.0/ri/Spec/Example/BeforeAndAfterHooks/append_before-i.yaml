--- !ruby/object:RI::MethodDescription 
aliases: 
  - !ruby/object:RI::AliasName 
    name: before
block_params: 
comment: 
  - !ruby/struct:SM::Flow::P 
    body: Registers a block to be executed before examples. <tt>scope</tt> can be <tt>:each</tt> (default), <tt>:all</tt>, or <tt>:suite</tt>. When <tt>:each</tt>, the block is executed before each example. When <tt>:all</tt>, the block is executed only once before any examples are run.
full_name: Spec::Example::BeforeAndAfterHooks#append_before
is_singleton: false
name: append_before
params: (scope = :each, &block)
visibility: public
